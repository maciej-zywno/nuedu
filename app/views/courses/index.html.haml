%h1 Listing courses
- @courses.in_groups_of(3, false) do |group| 
  .row
    - group.each do |course|
      .col-sm-6.col-md-4
        .thumbnail
          = image_tag course.logo.url(:medium) 
          .caption
            %h3=course.name
            %p
              = link_to 'Show more', course, class: 'btn btn-primary'
              = link_to 'Enroll', course, class: 'btn btn-default'
